<%= request.query.inspect %>
<%
@schema.delete request.query['name']
@db.each_key do |key|
	@db[key].delete request.query['name']
end
@order.delete request.query['name']

save_db @db, @schema, @order, @file
response.redirect("/?m=Property%20Deleted")
%>
