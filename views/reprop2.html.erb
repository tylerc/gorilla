<%= request.query.inspect %>
<%
order = []
p request.query
request.query.each do |pair|
	order[pair[1].to_i] = pair[0]
end
p order
%>
<%= @order.inspect %>
<%= order.inspect %>
<%
@order = order.clone
save_db @db, @schema, @order, @file
response.redirect("/?m=Properties%20Rearranged")
%>
